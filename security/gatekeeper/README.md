# Gatekeeper Policy Template

This is a template to demonstrate policy enforcement as an extra-credit deliverable.
To make it enforce in a real cluster:
1) Install Gatekeeper
2) Apply a ConstraintTemplate for required labels
3) Apply the Constraint

Included here:
- constraint.yaml (example constraint referencing a common template name)
